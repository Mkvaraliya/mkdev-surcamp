-------------------------------------------------------------------------------------------------------------------

Strapi account credentials : -

email : mahediali.k.varaliya456@gmail.com
password : Mehdi@Strapi@786

-------------------------------------------------------------------------------

Use below command to create next js project : - 
npx create-next-app@latest

Now run the below commands then we will run the next js project: -

cd client
npm run dev

-------------------------------------------------------------------------------

Use below command to create strapii project : -
npx create-strapi-app@latest server

Now do the below commands then we will run the stripe project & signup if new to stripe : -

cd server
npm run develop

-------------------------------------------------------------------------------------------------------------------

Use below command to setup git for your project : - 

cd client
rm -rf .git
cd ..
cd server
rm -rf .git
cd ..
clear

After doing the above commands copy the below commands and paste and run them : -

echo "# mkdev-surcamp" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/Mkvaraliya/mkdev-surcamp.git
git push -u origin main